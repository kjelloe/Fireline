# Session report 2026-08-03/04 — the contact-law era

The merged team-keyed hunt — open since the obstacle-slide era — is
essentially won. Two structural asymmetries of the same class were
convicted and fixed, and the default war is **in band both worlds for
the first time since the hunt opened**.

## Slices landed

| Commit | What |
|---|---|
| `a8f37c2` | THE REFRAME: all scout combat happens on A's half — the front line sits west; the line instrument designed |
| `b5b5a69` | THE LINE ROOT: meeting-stop law convicted (sequential collision checks let the decisive tick's first mover park one speed quantum deeper — 88 v 143 measured) and fixed with start-of-tick snapshot verdicts; mirror pin red-then-green |
| `a81477c` | THE PHASE LOCK, EMISSION EDITION: Q18's lead-team law was raw tick parity — a fixed team led every mirror-synchronized exchange; Q71's ORDERPARITY was a dead switch all along (it enforced the sort's own order). Fixed: lead team = seeded hash bit of (tick, mapSeed) |
| `b8a3e04` | O4: named `--difficulty easy\|normal\|hard` + the join screen shows the host's AI level (s_lobby, both locales) |
| `c56b3b0`/`3dd35c1` | Docs, skill laws, plans, memories synced |

Suite 780/780 (double-run), smoke incl. the 2D-join gate, acceptance,
5-seed gates on both engine changes. 1A fixture untouched.

## The measured verdict (n=300 pairs, a81477c)

| Config | Last era | b5b5a69 only | a81477c (both fixes) |
|---|---|---|---|
| frontier_uq | 42.8 / 44.0% A | 39.7 / 40.3 | **54.0 / 52.9 — IN BAND BOTH WORLDS** |
| POWS=2 | 25.9 / 28.3% A | 25.9 / 28.3 | **39.4 / 36.4** (+~12 pts, still below band) |

The two channels interact — each fix alone read flat or worse. The
b5b5a69-only column is the proof the batteries had to be same-build.

## New laws on record (sim-campaign skill)

- **Dead-switch law**: an A/B arm returning bit-identical counts is a
  dead switch (no-op or unplumbed), never an "inert" verdict — verify
  the mechanism engages first. (ORDERPARITY was dead all along; the
  historical "tick-parity inert" ruling judged nothing.)
- **Phase-lock class**: raw tick-parity alternation is NOT fair for
  mirror-synchronized events — symmetric routes put the decisive
  moment on a fixed parity every war. Two convictions: march order
  (fixed b5b5a69), emission order (fixed a81477c). Audit any future
  parity-alternated order.

## Open items

1. **POWS=2 residual** (~12 pts): the line instrument on the new
   build shows the war even through t=4000, then a one-sided A-loss
   run t≈4250–5000 before A's lone MPG wave. **The mid-war ledger is
   the next instrument** (per-event census of the window, then the
   naming kill-switch A/B).
2. **Era invalidation**: sawtooth/riverline/caldera/convoy/heist
   numbers are all last-era — the re-baseline slate is queued on the
   PC lane (results will state which per-profile verdicts survive).
3. **Band watch**: frontier now sits in the band's upper half
   (54/53 — slight A lean, inside tolerance).
4. Filed: MPG wave-timing asymmetry (B refills earlier despite fewer
   losses); the east curse (~9 pts side-keyed, riverline+convoy);
   ws flake (4 sightings).

## Questions for you

- **Q74**: frontier's slight A lean (54.0/52.9, upper band) — accept
  as tolerance, or keep hunting once the POWS residual closes?
- **Q75**: sawtooth was HELD at ~68% A on last-era physics. If the
  re-baseline comes back materially different, does the bench ruling
  get revisited, or does the human-playtest plan stand regardless?
- **Q76 (from the mid-war ledger)**: the POWS residual's shape is a
  REBUILD OVERSHOOT — the team that loses its artillery early gets
  its MPG wave sooner, and the fresh wave outguns the "winner's"
  attrition-worn survivors (at POWS=0 the cycle stays coupled and
  self-corrects; the POW carrier/crawl spiral is the amplifier). Is
  that comeback depth intended, or should wave strength/timing scale
  with the pool difference? Same-build RAIDPARTY/POWARC bisections
  are queued to name the decoupler precisely.
