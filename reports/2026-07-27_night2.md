# Night Session 2 — 2026-07-27 (branch `dev_night`, 7-hour window)

*Living document. Constraint: NIGHTLY-SAFE only — the batch PC is running
the sweep census on its pinned tag, so nothing lands tonight that would
make those results stale on arrival: presentation, client, tools, tests,
docs, and at most behavior-neutral plumbing.*

## Opening state

- Suite **447/447** (fuzz test just added), fixture v34, tags through
  `slice-14j`. Factions live, playtest-6 UX batch complete.
- Nightly running on the batch PC: sweep 600 → factionswap 300 →
  mirror 600 (+ perf). Analyzer ready for the morning CSVs.
- Awaiting from the user: chevron verdict (east-facing question),
  nightly CSVs, item-7 garage-config ruling.

## The queue (in order)

| # | Item | Why it's safe |
|---|---|---|
| 1 | ✅ Review round: command fuzzer (3000 adversarial commands, no-throw + determinism), doc/memory sync | tests only |
| 2 | **15A mobile touch** (ruled in Q10: on-screen steering arrows → drive-intent, tap unit to stop, tap orders, pinch zoom) | client-only; pure touch-mapper module, headless-tested |
| 3 | **Encyclopedia** (playtest 6.1 "add as an option button at some point"): full codex browser — every chassis + core mechanics pages | client-only; reads the existing codex module |
| 4 | **Strings part 3**: the last en-only surface (index.html static text, join page) | client-only |
| 5 | *(optional, behavior-neutral)* Item-7 plumbing: garage/MPG params into hashed rules-state with defaults IDENTICAL to today — tuning lands later on nightly data | engine, but byte-identical defaults; repin routine |
| 6 | *(only if promoted — see clarifications)* 14C motion pass | client-only |

## Clarifications for the user (none block the queue)

1. **Motion pass (art 2b) and baked sprites (2d)** are still "noted for
   later" from prompt 25. Promote either for tonight/tomorrow?
2. **Mobile**: I can build and headless-test the whole touch layer, but
   the FEEL verdict needs your phone/tablet — which device should the
   first mobile playtest target (affects layout breakpoints)?
3. **Item-7 defaults**: if I plumb garage/MPG config tonight
   (behavior-neutral), proposed difficulty presets are easy = threshold 8
   / 60 s cadence, normal = 6 / 90 s (today), hard = 4 / 150 s — veto or
   adjust whenever.

## Landed this session

*(updated as the night proceeds)*

- Review round: `test/fuzz_commands.test.js` — the reducer survives 3000
  seeded-adversarial commands without throwing and stays byte-
  deterministic under abuse. Suite 447/447.
