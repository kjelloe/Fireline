# 8-Hour Session Report — 2026-07-27 (branch `dev_night`)

Suite **466/466** (double-run), fixture v35 (untouched — nothing moved the
reducer). Five commits: `slice-16a`, `slice-13g`, `16a verification`,
riverline tooling, `slice-16b` (dormant) + batch job kinds.

## Headline: the war is FAIR now (16a — your GO, delivered)

The tick-parity fix landed, gated, and verified at 1200-war scale:
- AI doctrine commands now alternate the lead team by tick parity
  (`ai_regency.js`, emission loop only; roles stay stable-ordered).
- **Before** (2000-war PC soak, pre-fix baseline): A 56.7% of decided.
- **After** (600 normal + 600 mirrored local): **A 49.8% / B 50.2%** —
  and the analyzer's verdicts were rewritten so future nightlies read
  correctly (per-seed flip rate measures correlation, not bias; the
  team-bias tell is the same team keeping its edge in both worlds).
- 5-seed gate healthy, `test/tick_parity.test.js` pins the alternation
  from the authoritative command log. No repin needed (no AI in 1A).
- **PC re-confirmation at scale queues after you push** (sweep + mirror).

## Difficulty presets (13G — your numbers, landed)

`RULES=easy|normal|hard npm start`. easy 8/600, normal 6/900 (= today,
identity-pinned to DEFAULT_RULES), hard 4/1500. Found and fixed a real
regression while wiring: **createAppServer silently dropped mapProfile
AND rules** — `MAP=riverline npm start` served frontier. Regression test
now drives the app layer with riverline+hard and reads the state back.

## Riverline census (300 + 300 mirrored): two verdicts

1. **Standard runs are viable** — 19% of wars end by standard capture
   (frontier: 27%). Riverline is slower (18% undecided at the horn,
   double the mines, a third of the downs) but NOT horn-locked.
2. **The east side is stronger.** B wins 58.9% of decided — and it
   FLIPS perfectly under world-mirroring (aggregate 50.4%). Engine
   fair, doctrine fair; the seed-generated terrain systematically
   favors the east. Since teams are bound to sides, riverline needs a
   terrain symmetrization pass eventually (design queue).

## Faction uniques (16B): built, gated, shipped DORMANT

A probe showed the Sentinel/Skimmer are dead content in AI wars — never
crewed, so the anchor doctrine I wrote never fired. Landed behind a
flag (`uniqueCrewing` option / `UNIQUES=1` sweeps), default byte-
identical (test-pinned + 5-seed gate byte-identical):
- Crewing rung (junior to carrier/courier/tube; fixed agents' own seat
  outranks it) + REACTIVE Sentinel fortress doctrine (deploy only when
  anchored AND the team sees an enemy in deployed reach).
- Evidence chain that kept it dormant: standing fortress swept **78/19**;
  swap gate convicted the chassis (Sentinel-side 67.4%); the reactive
  tune passed the chassis gate (**53.1%**) — but a **~12pt TEAM-linked
  A-edge remains** (59.7% aggregate across mirror worlds on seeds whose
  baseline is 47.5%). Mechanism not isolated; hypothesis: fastest-seat
  recoverer distortion by the Skimmer + an unexplained A-keyed component.
  It fails the sim gate, so it does not default on. `uniques` batch job
  kind added (`swap`/`mirror` variants) to chase it at PC scale.

## Batch PC: 8 hours of baseline data banked + pipeline hardened

- 2000-war soak + 300-war hard-AI matrix ran and mailed home (hard AI:
  ZERO undecided, median war 3474 ticks, 244 dominations — great
  material for the presets narrative).
- **perf job FAILED: Playwright is not installed on the PC.** One-time
  fix there: `npx playwright install chromium` in the repo, then re-queue.
- Collector had a real bug its own test mirrored (read "body" where the
  store says "text") — fixed; the test now embeds a verbatim live-store
  line so fixture drift is impossible.
- Worker refusal message now lists this-checkout job kinds (it is the
  version handshake).

## Your morning sequence

1. `git push` (5 slices on dev_night).
2. On the PC: `npx playwright install chromium` (once), then
   `git pull && bash tools/batch_worker.sh`.
3. Tell me — I queue: sweep 600 + mirror 600 (16a at scale), riverline
   600, uniques 300×4 (normal/swap/mirror/both), perf. All results mail
   home automatically now.

## Open items for you (none block anything)

- Chevron verdict from your next playtest (east-facing question).
- Motion pass / baked sprites promotion (still "noted for later").
- Riverline terrain symmetrization — design appetite? (new finding)
- Which mobile device for touch feel tuning.
